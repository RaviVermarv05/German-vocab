# === Core System & Environment ===
python-dotenv==1.0.1     # For loading API keys from .env
requests==2.32.3          # For calling external APIs (e.g., PONS)

# === Machine Learning & Data ===
pandas==2.2.2             # Data management
scikit-learn==1.5.2       # ML models (Naive Bayes, vectorizers, etc.)
joblib==1.4.2             # For saving/loading trained ML models

# === AI & Language Model Integration ===
groq==0.5.0               # Groq API client for LLM-based descriptions

# === Audio & Speech ===
pygame==2.6.1             # For playing sound effects
gTTS==2.5.3               # For generating spoken vocabulary audio

# === Utility / Development ===
tqdm==4.66.5              # Optional: progress bars for longer tasks


Create a .env file in your project root and add:

# Groq AI Key (get from https://console.groq.com/keys)
GORQ_API_KEY=your_groq_api_key_here

# PONS Dictionary API Key (get from https://en.pons.com/open_dict/public_api/secret)
PONS_API_KEY=your_pons_api_key_here
